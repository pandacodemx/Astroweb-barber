---
const navItems = [
  { name: "Inicio", href: "#home" },
  { name: "Servicios", href: "#services" },
  { name: "Galería", href: "#gallery" },
  { name: "Reservas", href: "#booking" },
];
---

<header class="fixed w-full bg-gray-500/10 backdrop-blur-lg z-50 border-b border-amber-600/20">
  <nav class="container mx-auto px-6 py-4 flex justify-between items-center">
    <a href="#home" class="text-2xl font-bold text-amber-500">BARBER<span class="text-white">VINTAGE</span></a>
    
    <div class="hidden md:flex space-x-8">
      {navItems.map(item => (
        <a href={item.href} class="hover:text-amber-500 transition-colors">{item.name}</a>
      ))}
    </div>
    
    <button class="md:hidden">
      <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
      </svg>
    </button>
  </nav>
</header>